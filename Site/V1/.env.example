# Optionnel : origine du backend Linkage (sans slash final). Surcharge le repli prod (Render) dans apiBase.ts.
# Laisser vide en local : le proxy Vite envoie /api → http://localhost:4000
VITE_API_BASE_URL=
