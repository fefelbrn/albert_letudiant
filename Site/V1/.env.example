# Obligatoire sur Vercel : origine du backend Linkage (Render, etc.), sans slash final.
# Exemple si l’API est sur Render :
# VITE_API_BASE_URL=https://albert-letudiant.onrender.com
# Laisser vide en local : le proxy Vite envoie /api → http://localhost:4000
VITE_API_BASE_URL=
